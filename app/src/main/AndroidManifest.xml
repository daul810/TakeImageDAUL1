<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nidaul.photos">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/camera"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/camera"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.nidaul.photos.SPhoto"></activity>
        <activity android:name="com.nidaul.photos.PhotoLain"></activity>
        <activity android:name="com.nidaul.photos.MainActivity">
        </activity>
            <activity android:name="com.nidaul.photos.SplashActivity">
                <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>
    </application>

</manifest>